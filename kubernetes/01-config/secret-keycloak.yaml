---
# WARNING: Development Environment Only
# These secrets are base64-encoded (NOT encrypted)
# DO NOT use in production environments
# Production requires: Kubernetes Secrets encryption, external secret stores
# (e.g., Azure Key Vault, AWS Secrets Manager, HashiCorp Vault)

apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin
  namespace: api-gateway-poc
  labels:
    app: keycloak
type: Opaque
data:
  # admin / admin (base64 encoded)
  # To generate: echo -n 'admin' | base64
  username: YWRtaW4=
  password: YWRtaW4=
