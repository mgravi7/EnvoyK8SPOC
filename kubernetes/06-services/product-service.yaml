---
# Product service ClusterIP service (internal only)
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: api-gateway-poc
  labels:
    app: product-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
